{
    "devDependencies": {
        "jest": "^22.1.4",
        "web-ext": "^2.3.1"
    },
    "scripts": {
        "test": "jest",
        "build": "web-ext build",
        "start": "web-ext run"
    }
}
