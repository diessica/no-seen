{
    "devDependencies": {
        "jest": "^27.5.1",
        "web-ext": "^6.8.0"
    },
    "scripts": {
        "test": "jest",
        "build": "web-ext build",
        "start": "web-ext run"
    }
}
